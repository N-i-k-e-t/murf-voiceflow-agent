# Demo Video

Record a short screen+audio capture showing:

1. Running `python -m app.cli_runner`
2. Speaking 2–3 queries
3. Hearing real-time Murf Falcon responses

Save the video here as `demo.mp4` and mention this path in the main README.
